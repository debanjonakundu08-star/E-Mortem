# For local dev, Vite proxies /api to FastAPI at http://localhost:8000
# On Vercel, relative /api is automatically used for Vercel Serverless Functions
# Set VITE_API_URL in Vercel Environment Variables only if using an external API host
VITE_API_URL=
